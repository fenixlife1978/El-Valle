rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // ATENCIÓN: Esto permite el acceso público total.
      // Ideal para desarrollo, pero debe ser asegurado para producción.
      allow read, write: if true;
    }
  }
}
