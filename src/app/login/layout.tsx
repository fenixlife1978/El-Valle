import type { ReactNode } from 'react';

export const metadata = {
  title: 'Login | EFAS Condosys',
};

export default function LoginLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
